{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && npm install",
    "dev:client": "cd client && npm start",
    "nodemon:client": "cd client && nodemon",
    "preinstall": "cd server && npm install",
    "dev:server": "cd server && npm start",
    "nodemon:server": "cd server && nodemon",
    "start": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:start": "concurrently \"npm run nodemon:client\" \"npm run nodemon:server\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
